install:
  build: latest
  images:
    cleanup: true
    repos: true
    task: rpm
  mirror: tlv
  quickstart:
    filename: undercloud-quickstart.qcow2
  ssl: false
  user:
    name: stack
    password: stack
  version: '10'
