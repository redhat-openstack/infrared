collect_ansible_facts_dir_tmp: "{{ ansible_user_dir }}/ansible-facts_tmp"
collect_ansible_facts_dir_dest: "{{ inventory_dir }}/ansible-facts" # so we capture these files as part of infrared workspace
collect_ansible_facts_filename: "infrared-ansible-facts"