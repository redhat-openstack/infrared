This should be place dedicated to tracking
git hooks, which are by design not trackable
using standard .git directory.

It is expected they will be manually linked
into <InfraRed>.git/hooks/ directory as part
of user's setup of the git/gerrit environment.

More on https://infrared.readthedocs.io/en/latest/bootstrap.html
