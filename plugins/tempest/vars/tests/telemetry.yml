---
test_dict:
    test_regex:
    whitelist:
        - "^tempest.api.telemetry.*"
        - "^telemetry_tempest_plugin.*"
    blacklist: []
    plugins: "http://git.openstack.org/openstack/telemetry-tempest-plugin"
        package:
            default: "python-telemetry-tests-tempest"
