---
test_dict:
    test_regex:
    whitelist:
        - "^tempest.api.telemetry.*"
        - "^telemetry_tempest_plugin.*"
    blacklist: []
    plugins:
        telemetry:
            repo: "https://opendev.org/openstack/telemetry-tempest-plugin"
            package:
                default: "python-telemetry-tests-tempest"
