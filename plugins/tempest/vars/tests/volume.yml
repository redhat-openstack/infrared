---
test_dict:
    test_regex: ''
    whitelist: []
        - "^tempest.api.volume.*"
        - "^cinder.tests.tempest.*"
    blacklist: []
    plugins:
        cinder:
            repo: "https://github.com/openstack/cinder.git"
            package:
                default: "python-cinder-tests"
