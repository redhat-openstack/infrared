---
test_dict:
    test_regex: 'kuryr'
    whitelist: []
    blacklist: []
    plugins:
        kuryr:
            repo: "https://github.com/openstack/kuryr.git"
            package:
                default: "python-kuryr-tests"
