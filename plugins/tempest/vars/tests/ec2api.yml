---
test_dict:
    test_regex:
    whitelist:
        - "^ec2api.*"
        - "^ec2api_tempest_plugin.*"
    blacklist: []
    plugins:
        ec2api_tests:
            repo: "http://git.openstack.org/cgit/openstack/ec2api-tempest-plugin"
            package:
                default: "python-ec2api-tests-tempest"
