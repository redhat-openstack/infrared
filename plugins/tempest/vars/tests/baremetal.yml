---
test_dict:
    test_regex: tempest.api.baremetal
    whitelist:
        - "^tempest.api.baremetal.*"
        - "^ironic_tempest_plugin.*"
    blacklist: []
    plugins:
        ironic:
            repo: "http://git.openstack.org/cgit/openstack/ironic-tempest-plugin"
            package:
                default: "python-ironic-tests-tempest"
