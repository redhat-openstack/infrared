---
test_dict:
  test_regex:
    default: ''
  whitelist:
    - "^tempest.api.identity.*"
    - "^keystone_tempest_plugin.*"
  blacklist: []
  plugins:
    keystone-tempest-plugin:
      repo: "https://opendev.org/openstack/keystone-tempest-plugin"
      package:
        default: "python3-keystone-tests-tempest"
