---
test_dict:
    test_regex: ''
    whitelist:
        - kuryr_tempest_plugin.tests.scenario.test_cross_ping.TestCrossPingScenario.test_pod_pod_ping
    plugins:
      kuryr-tempest-plugin:
        repo: "https://github.com/openstack/kuryr-tempest-plugin.git"
