---
test_dict:
    test_regex:
        default: "^keystone-tempest-plugin"
    whitelist: []
    blacklist: []
    plugins:
      identity-tests:
        repo: "https://opendev.org/openstack/keystone-tempest-plugin"
        package:
          default: "python3-keystone-tests-tempest"
