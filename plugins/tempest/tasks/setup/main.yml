- name: Include setup vars
  include_vars: "vars/setup/{{ test.setup }}.yml"

- name: Setup tempest
  include_tasks: "{{ test.setup }}/main.yml"

- name: apply workarounds (if any)
  include_tasks: "workarounds.yml"
