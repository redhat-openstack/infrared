---
- name: Include test vars
  include_vars: "{{ test_suite_file }}"

- name: Install plugin packages for "{{ test_suite_file }}" suite
  become: yes
  package:
      name: "{{ item.value.package }}"
      state: present
  with_dict: "{{ test_dict.plugins | default({}) }}"
