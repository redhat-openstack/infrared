---
- fail:
      msg: "Upgrade is supported only for version 9 and 10"
  when:
    - install.upgrade|default(false)
    - not (undercloud_version == "9")
    - not (undercloud_version == "10")

- fail:
      msg: "Update is supported only for version 11"
  when:
    - install.get('update', {}).undercloud|default(false)
    - not (undercloud_version == "11")
