# FIXME(yfried): Ansible 2.2 allows to define dict nesting destination in module
# so "ssl" level should be removed
ssl:
    privkey: undercloud-privkey.pem
    cacert: /etc/pki/ca-trust/source/anchors/undercloud-cacert.pem
    final_cert: /etc/pki/instack-certs/undercloud.pem
