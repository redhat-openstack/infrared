containers:
    puddle_base_url: http://private_url/rcm-guest/puddles/OpenStack/12.0-RHEL-7/
    # set override_puddle if you want to use currently installed puddle
    # override_puddle: latest_containers

    # file names below are relative to the puddle url
    container_images_file_name: container_images.yaml
    docker_template_file_name: docker-osp12.yaml

    docker_insecure_registry: private_docker_registry_url
