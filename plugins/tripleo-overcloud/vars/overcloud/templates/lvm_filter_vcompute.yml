---
tripleo_heat_templates:
    - "{{ install.heat.templates.basedir }}/environments/lvm_filter_vcompute.yaml"

custom_templates:
    parameter_defaults:
        ComputeParameters:
            LVMFilterEnabled: true
            LVMFilterAllowlist:
                - /dev/vda
