---
tripleo_heat_templates:
    - "{{ install.heat.templates.basedir }}/environments/{% if (install.version | default(undercloud_version) | openstack_release <= 13)  %}manila-netapp-config.yaml{% else %}manila-netapp-config-docker.yaml{% endif %}"
