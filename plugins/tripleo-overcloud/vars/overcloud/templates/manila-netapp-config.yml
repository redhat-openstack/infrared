---
tripleo_heat_templates:
    - "{{ install.heat.templates.basedir }}/environments/{% if (install.version | default(undercloud_version) | openstack_release > 12) %}manila-netapp-config-docker.yaml{% else %}services{% endif %}/manila-netapp-config.yaml"
