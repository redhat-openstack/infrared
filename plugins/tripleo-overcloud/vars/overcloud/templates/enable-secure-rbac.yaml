---
tripleo_heat_templates:
  - "{{ install.heat.templates.basedir }}/environments/enable-secure-rbac.yaml"

custom_templates:
  parameter_defaults:
    EnforceSecureRbac: "{{ (install.version|default(undercloud_version)|openstack_release >= 17)|ternary('True', 'False') }}"
