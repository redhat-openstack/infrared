custom_templates:
  parameter_defaults:
      HostnameMap:
          ceph-0: ceph-0-{{osp_version | replace('.','-')}}
          compute-0: compute-0-{{osp_version | replace('.','-')}}
          compute-1: compute-1-{{osp_version | replace('.','-')}}
          controller-0: ctrl-0-{{osp_version | replace('.','-')}}
          controller-1: ctrl-1-{{osp_version | replace('.','-')}}
          controller-2: ctrl-2-{{osp_version | replace('.','-')}}
