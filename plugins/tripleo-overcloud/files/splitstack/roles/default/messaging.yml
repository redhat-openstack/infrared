messaging_role:
    name: MessagingDeployedServer

    resource_registry:
        "OS::TripleO::MessagingDeployedServer::Net::SoftwareConfig": "${deployment_dir}/network/nic-configs/messaging${ipv6_postfix}.yaml"