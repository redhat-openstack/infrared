compute_role:
    name: ComputeDeployedServer

    resource_registry:
        "OS::TripleO::ComputeDeployedServer::Net::SoftwareConfig": "${deployment_dir}/network/nic-configs/compute${ipv6_postfix}.yaml"