networker_role:
  name: NetworkerDeployedServer

  resource_registry:
      "OS::TripleO::NetworkerDeployedServer::Net::SoftwareConfig": "${deployment_dir}/network/nic-configs/networking${ipv6_postfix}.yaml"