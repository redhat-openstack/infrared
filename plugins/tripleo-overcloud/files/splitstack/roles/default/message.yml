compute_role:
  name: MessageDeployedServer

  resource_registry:
      "OS::TripleO::MessageDeployedServer::Net::SoftwareConfig": "${deployment_dir}/network/nic-configs/messaging${ipv6_postfix}.yaml"