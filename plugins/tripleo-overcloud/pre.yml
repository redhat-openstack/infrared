- name: Placeholder for the undercloud pre-tasks
  hosts: undercloud
  gather_facts: yes
  tags: init
  any_errors_fatal: true
  tasks:
       - name: URL to download package
         yum:
             name: {{ install.download.url }}
             state: present
         when:
             - install.download is defined
             - install.download.url
         #ignore_errors: yes
