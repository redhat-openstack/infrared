- name: Placeholder for the undercloud pre-tasks
  hosts: undercloud
  gather_facts: yes
  tags: init
  any_errors_fatal: true
  tasks:
       - name: URL to download package
         yum:
             name: "{{ install.undercloud.packages }}"
             state: present
         when:
             - install.undercloud is defined
             - install.undercloud.packages
         become_method: sudo
         register: yum_result
         failed_when: "'FAILED' in yum_result.stderr"
