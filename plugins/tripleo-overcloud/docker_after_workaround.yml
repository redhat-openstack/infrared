---
- name: Disable Compute se-linux State(without changes to tripleo puppet)
  #(skatlapa) For all OC nodes
  hosts: compute
  become: true
  gather_facts: no
  #tags: init
  any_errors_fatal: true
  tasks:
      - name: "setenforce shell use for compute"
        shell: |
           setenforce 0