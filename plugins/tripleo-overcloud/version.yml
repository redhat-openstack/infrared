- name: Get the undercloud version
  hosts: "{{ (_run_condition | bool) | ternary('undercloud', 'none') }}"
  gather_facts: yes
  any_errors_fatal: true
  tags:
      - images
  roles:
      - version-discovery
