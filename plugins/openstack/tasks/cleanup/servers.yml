- name: Cleanup servers
  os_server:
      cloud: "{{ provision.cloud | default(omit) }}"
      name: "{{ item.id }}"
      state: absent
      delete_fip: yes
  with_items: "{{ lookup('file', inventory_dir + '/resources/servers.yml') | from_yaml }}"
  register: servers_cleanup
