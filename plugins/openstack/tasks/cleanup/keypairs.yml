- name: Cleanup keypairs
  os_keypair:
      cloud: "{{ provision.cloud | default(omit) }}"
      name: "{{ item.id }}"
      state: absent
  with_items: "{{ lookup('file', inventory_dir + '/resources/keypairs.yml') | from_yaml }}"
  register: keypairs_cleanup
