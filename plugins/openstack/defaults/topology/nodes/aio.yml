node_dict:
    name: aio
    flavor: "m1.xlarge"
    interfaces:
        nic1:
            network: "data"
        nic2:
            network: "management"
        nic3:
            network: "external"
    external_network: management

    groups:
        - controller
        - tester
        - network
        - compute
        - openstack_nodes
        - overcloud_nodes
