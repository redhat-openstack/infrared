name: standalone
flavor: "g.standard.xl"
interfaces:
    nic1:
        network: "data"
external_network: data

security_groups:
    - general_access

groups:
    - undercloud
    - standalone
    - openstack_nodes
