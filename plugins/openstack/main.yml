---
- import_playbook: cleanup.yml

- name: plugins/openstack
  hosts: localhost
  gather_facts: no
  tasks:

    - name: provisioning tasks
      include_tasks: provision.yml
      when: not provision.cleanup|default(False)
