- name: Locking OpenStack instances
  hosts: localhost
  gather_facts: yes
  any_errors_fatal: true
  vars:
  tasks:
      - name: Lock instance
        os_server_action:
          cloud: "{{ provision.cloud | default(omit) }}"
          action: lock
          server: "{{ item }}"
        loop: "{{ provision.instances }}"
