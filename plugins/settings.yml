provisioner:
  root_dir: ./plugins/ir-provision
  config:
    settings: settings
    modules: library
    roles: roles
    playbooks: playbooks
  entry_points:
    playbook:  provision.yml
    cleanup: cleanup.yml

