---
- name: create bridge
  command: "ip link add {{ item.network | format(num) if item.needs_formatting | default(False)  else item.network }}"
  ignore_errors: true
  with_items: "{{ topology_node.node_indexes }}"
  loop_control:
    loop_var: num
