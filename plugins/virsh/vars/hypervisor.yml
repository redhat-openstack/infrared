packages:
    - libvirt
    - libvirt-python
    - libguestfs
    - libguestfs-tools-c
    - libguestfs-xfs
    - python-lxml
    - qemu-kvm
    - virt-install
    - wget
    - libselinux-python

custom_facts: "/etc/ansible/facts.d/"
