packages:
    - expect
    - libguestfs
    - libguestfs-tools-c
    - libguestfs-xfs
    - libselinux-python
    - libvirt
    - libvirt-python
    - python-ipaddr
    - python-lxml
    - qemu-kvm
    - virt-install
    - wget

custom_facts: "/etc/ansible/facts.d/"
