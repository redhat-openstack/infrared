packages:
    - expect
    - libguestfs
    - libguestfs-tools-c
    - libguestfs-xfs
    - libselinux-python
    - libvirt
    - libvirt-python
    - python-lxml
    - qemu-kvm
    - virt-install
    - wget

openvswitch_packages:
    - openvswitch
    - NetworkManager-ovs

custom_facts: "/etc/ansible/facts.d/"
