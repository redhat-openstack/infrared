memory_buffer: 1.2
ignore_virsh_nets:
    - default

packages:
    - libvirt
    - libvirt-python
    - libguestfs
    - libguestfs-tools-c
    - libguestfs-xfs
    - python-lxml
    - qemu-kvm
    - virt-install
    - wget
    - libselinux-python
    - expect

custom_facts: "/etc/ansible/facts.d/"
