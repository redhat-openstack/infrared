rally:
    dir: "{{ ansible_user_dir }}/rally_repo"
    path: "{{ ansible_user_dir }}/rally"
    outputdir: /tmp/rally_output
    bin: "rally"
setup:      
    dependencies:
        defaults:    
        # This version of ansible is required by python-os-faults-0.1.11-2 package
            - ansible-2.3.2.0-2.el7
