config:
    bin: "{{ ansible_user_dir }}/rally/bin/rally"
setup:
    packages:
        - git
        - wget
        - which
        - gcc
        - libffi-devel
        - libxml2-devel
        - libxslt-devel
        - openssl-devel
        - python3-setuptools
        - postgresql-devel
        - gmp-devel
    pip_packages:
        - requests
        - six