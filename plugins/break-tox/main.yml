---
- name: nothing here
  hosts: all:!localhost:!hypervisor
  gather_facts: yes
  become: yes
  any_errors_fatal: false  # as we are happy to collect whatever we can
  tasks:
      - command: SOME=var echo here
