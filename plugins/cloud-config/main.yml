---
- name: Temporary main playbook for 'cloud-config' plugin
  hosts: localhost
  gather_facts: no
  any_errors_fatal: true
  tasks:
      - name: validate storage nodes
        debug:
            msg: "Dummy plugin task"
