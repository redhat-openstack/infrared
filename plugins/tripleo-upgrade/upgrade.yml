- name: Set plugin_dir
  hosts: undercloud
  gather_facts: yes
  any_errors_fatal: true
  tags: upgrade
  tasks:
    - set_fact : plugin_dir="{{ playbook_dir }}"

- name: Version 10 Upgrade
  include: "tasks/{{ install.version }}/upgrade.yml"