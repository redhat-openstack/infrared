---
- name: Fetch results
  hosts: tester
  become: yes
  become_user: "{{ installer.user.name }}"
  gather_facts: no
  tasks:
      - fetch:
          src: "{{ tester.component.dir }}/horizon.xml"
          dest: "{{ inventory_dir }}/nosetests.xml"
          flat: yes
