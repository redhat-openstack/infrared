# Setup BMC node
- include: packages.yml
- include: deploy_files.yml
- include: os-net-config.yml

# Deploy service file for each node in 'fake_baremetal_nodes' group
- include: openstack-bmc.yml
  with_dict: "{{ provisioner.nodes }}"
