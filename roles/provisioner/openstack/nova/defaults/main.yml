# General defaults
prefix: "{{ provisioner.prefix | default(random.uuid) }}"

# Instances defaults
auto_ip: no
config_drive: no
instance_image: centos-minimal
wait: yes
default_ssh_user: cloud-user

# Flavor defaults
flavor_id: m1.large
flavor_name: ir-flavor
