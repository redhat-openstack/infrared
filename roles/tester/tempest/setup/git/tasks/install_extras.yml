- name: install extra system requirements for "{{ plugin_item.key }}"
  yum:
    name: "{{ item }}"
    state: present
  with_items: "{{ plugin_item.value.dependencies }}"
  when: plugin_item.value.dependencies is defined