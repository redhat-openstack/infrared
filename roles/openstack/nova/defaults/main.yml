# General defaults

prefix: "{{ provisioner.prefix | default(random.uuid) }}"

# Instances defaults

auto_ip: no
config_drive: no
instance_image: centos-minimal
wait: yes

# Flavor defaults

flavor_id: 4
flavor_name: ir-flavor
