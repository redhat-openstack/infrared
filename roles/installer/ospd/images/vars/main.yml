# Variables generated from default and role parameters
build_path: "{{ work_dir }}{{ ospd_version }}"
base_image: "{{ build_path }}/base_image.qcow2"
image_build_dir: "{{ build_path }}/built_images"
build_env_file: "{{ build_path }}/build-img-env-ospd{{ ospd_version }}"
build_log: "{{ build_path }}/openstack-build-images-{{ ospd_version }}.log"
tar_file: "ospd-oc-images-qe.tar.gz"
tar_download_url: "{{ storage_server }}/{{ storage_path }}/{{ ospd_version }}/{{ tar_file }}"
