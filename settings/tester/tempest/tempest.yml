---
tester:
    type: tempest
    dir: tempest-dir

job:
    archive:
        - '{{ tester.dir }}/etc/tempest.conf'
        - '{{ tester.dir }}/etc/tempest.conf.sample'
        - '{{ tester.dir }}/*.log'
        - '{{ tester.dir }}/*.xml'
