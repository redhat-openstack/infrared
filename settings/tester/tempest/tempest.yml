---
tester:
    type: tempest
    dir: tempest-dir
    downstream_dir: downstream-tempest

job:
    archive:
        - '{{ tester.dir }}/*.log'
        - '{{ tester.dir }}/*.xml'
        - '{{ tester.dir }}/etc/tempest.conf'
        - '{{ tester.dir }}/etc/tempest.conf.sample'
