---
#TODO: move everything out of isolation, have isolation as it's own option
installer:
    overcloud:
        network:
            isolation:
                enable: "yes"

defaults:
    protocol: ipv4
    type: "three-nics-vlan"
    variant: vxlan
