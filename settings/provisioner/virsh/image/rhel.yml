---
provisioner:
    image:
        # todo: same as in hyp
        base_url: "{{ !lookup private.images.rhel.7.2.remote_file_server }}"
        name: "rhel-guest-image-7.2-20151102.0.x86_64.qcow2"

        # TODO: move to topology
        memory: "16384"
        cpu: "4"
        os:
            variant: rhel7
        disk:
            size: "40G"

