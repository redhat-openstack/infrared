name: "ldap"
prefix: null
amount: "1"
cpu: "2"
memory: "4096"
os:
    type: "linux"
    variant: "rhel7"
disks:
    disk1:
        import_url: null
        path: "/var/lib/libvirt/images/ldap"
        dev: "/dev/vda"
        size: "20G"
        cache: "default"
        preallocation: "metadata"
network:
    interfaces:
        management:
            label: "eth0"
        data:
            label: "eth1"
        external:
            label: "eth2"
    floating_ip_network: management

groups:
    - ldap
    - openstack_nodes
    - overcloud_nodes
