test:
  cleanup: false
  config:
    options:
      images:
        http_image: http://rhos-qe-mirror-tlv.usersys.redhat.com/images/cirros-0.3.4-x86_64-uec.tar.gz
  dir: tempest-dir
  legacy:
    config: false
  openstack:
    installer: tripleo
    version: '10'
  revision: HEAD
  setup: rpm
  tests:
  - minimal
  threads: 8
